{
  "name": "discord-reaction-moderator",
  "version": "1.0.0",
  "description": "Cloudflare Worker that removes blocked emoji reactions from Discord messages",
  "main": "src/index.ts",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build:test": "tsc -p tsconfig.tests.json",
    "test": "sh -c 'rm -rf dist-tests; pnpm run build:test && node --test dist-tests/test/*.test.js; status=$?; rm -rf dist-tests; exit $status'"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.82.2"
  }
}
